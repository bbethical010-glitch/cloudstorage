You are a senior frontend + realtime systems engineer. You specialize in
React state management and WebRTC lifecycle control. You write
production-grade UI logic with clean separation of concerns.

Fix a UI bug where the “Finalizing Bridge” loading screen appears during
file uploads, even though the connection is already established.

App architecture: - React Web Console - WebRTC connection (primary) -
WebSocket relay fallback - Upload system recently refactored to
streaming

Observed issue: - Upload works correctly - But UI switches back to
“Finalizing Bridge…” screen during upload - Status still shows
“CONNECTED”

This screen should ONLY appear during initial connection setup.

Connection state and UI state are incorrectly coupled.

Upload or transport activity is triggering: - connection
re-initialization OR - connection state reset OR - component remount

This causes UI to incorrectly enter “connecting” state.

Refactor connection state management to ensure:

1.  Connection lifecycle is persistent and independent of uploads
2.  UI loading screen is shown ONLY during:
    -   initial connection
    -   explicit reconnect
3.  Upload operations DO NOT:
    -   reset connection state
    -   trigger loading UI

STEP 1 — Separate States

Create two independent states:

-   connectionState: “idle” | “connecting” | “connected” | “failed”

-   uiState: “loading” | “ready”

RULE: uiState = “loading” ONLY when connectionState === “connecting” AND
no prior successful connection

------------------------------------------------------------------------

STEP 2 — Add Persistent Flag

Add: hasConnectedOnce = useRef(false)

When connection becomes “connected”: hasConnectedOnce.current = true

UI RULE: IF hasConnectedOnce === true: NEVER show loading screen again

------------------------------------------------------------------------

STEP 3 — Prevent Reinitialization

Audit: - WebRTC initialization - signaling subscriptions - useEffect
dependencies

Fix: - Ensure connection setup runs ONLY once - Use stable singleton
(context or global store)

------------------------------------------------------------------------

STEP 4 — Fix React Effects

Common bug: useEffect(() => { initConnection() }, [uploadState]) ❌

Fix: useEffect(() => { initConnection() }, []) ✅

------------------------------------------------------------------------

STEP 5 — Stabilize Transport Layer

Ensure: - Upload does NOT recreate RTCPeerConnection - Upload does NOT
reopen WebSocket - Use existing connection instance

------------------------------------------------------------------------

STEP 6 — UI Rendering Logic

Replace:

if (connectionState !== “connected”) { return }

WITH:

if (!hasConnectedOnce.current && connectionState === “connecting”) {
return }

------------------------------------------------------------------------

STEP 7 — Optional UX Improvement

Instead of blocking screen during upload: - Show upload progress
overlay - Keep file browser visible

 - Must NOT break existing WebRTC flow - Must NOT introduce multiple
connections - Must handle reconnect correctly

Return:

1.  Updated React state management logic
2.  Fixed useEffect hooks
3.  Updated UI rendering conditions
4.  Any required context/store refactor

Include full working code (not snippets)

The fix is correct if: - Upload does NOT trigger loading screen -
Connection screen appears ONLY once at startup - UI remains in file
browser during upload - WebRTC connection is not recreated unnecessarily

 - Do not output pseudo-code - Do not explain outside code comments -
Focus on correctness and stability
